# Immich API 接口文档

## 上传资产接口

### 基本信息

- **接口地址**: `POST {baseUrl}/api/assets`
- **认证方式**: API Key (通过 `x-api-key` Header 传递)
- **请求格式**: `multipart/form-data`
- **响应格式**: JSON

### 请求 Headers

| Header | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `x-api-key` | string | 是 | Immich API 密钥 |
| `x-immich-checksum` | string | 否 | SHA1 校验和，用于重复检测 |

### 请求 Body (multipart/form-data)

#### 必填字段

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `assetData` | binary | 要上传的文件数据 |
| `deviceAssetId` | string | 设备资产ID，用于唯一标识 |
| `deviceId` | string | 设备ID，标识上传来源设备 |
| `fileCreatedAt` | string (ISO 8601) | 文件创建时间 |
| `fileModifiedAt` | string (ISO 8601) | 文件修改时间 |

#### 可选字段

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `filename` | string | 文件名 |
| `duration` | string | 视频时长 |
| `isFavorite` | boolean | 是否标记为收藏 |
| `livePhotoVideoId` | string (UUID) | 实况照片对应的视频ID |
| `sidecarData` | binary | 侧车数据（如EXIF信息） |
| `visibility` | string | 可见性，可选值: `archive`, `timeline`, `hidden`, `locked` |

### 响应

#### 成功响应 (201 Created)

```json
{
  "id": "string (UUID)",
  "status": "created",
  "duplicate": false
}
```

#### 重复资产响应 (200 OK)

```json
{
  "id": "string (UUID)",
  "status": "duplicate",
  "duplicate": true
}
```

#### 错误响应

- **400 Bad Request**: 请求参数错误
- **401 Unauthorized**: API 密钥无效
- **413 Payload Too Large**: 文件过大

### 示例代码

#### cURL 示例

```bash
curl -X POST "http://localhost:2283/api/assets" \
  -H "x-api-key: YOUR_API_KEY" \
  -H "x-immich-checksum: SHA1_CHECKSUM" \
  -F "assetData=@/path/to/image.jpg" \
  -F "deviceAssetId=image-001" \
  -F "deviceId=picgo-device" \
  -F "fileCreatedAt=2024-01-01T00:00:00.000Z" \
  -F "fileModifiedAt=2024-01-01T00:00:00.000Z" \
  -F "filename=image.jpg" \
  -F "isFavorite=false" \
  -F "visibility=timeline"
```

#### Python 示例

```python
import requests
import os
from datetime import datetime

url = "http://localhost:2283/api/assets"
headers = {"x-api-key": "YOUR_API_KEY"}

data = {
    "deviceAssetId": "image-001",
    "deviceId": "picgo-device",
    "fileCreatedAt": datetime.now().isoformat(),
    "fileModifiedAt": datetime.now().isoformat(),
    "filename": "image.jpg",
    "isFavorite": "false",
    "visibility": "timeline"
}

files = {"assetData": open("/path/to/image.jpg", "rb")}

response = requests.post(url, headers=headers, data=data, files=files)
print(response.json())
```

### 相关接口

#### 批量上传检查

- **接口地址**: `POST {baseUrl}/api/assets/upload-check`
- **用途**: 在上传前检查哪些资产已存在
- **请求体**:
  ```json
  {
    "assets": [
      {
        "id": "string",
        "checksum": "string (SHA1)"
      }
    ]
  }
  ```

#### 获取资产信息

- **接口地址**: `GET {baseUrl}/api/assets/{id}`
- **用途**: 获取指定资产的详细信息

#### 创建分享链接

- **接口地址**: `POST {baseUrl}/api/shared-links`
- **认证方式**: API Key (通过 `x-api-key` Header 传递)
- **请求格式**: JSON
- **用途**: 为上传的资产创建公开分享链接

**请求体**:

```json
{
  "type": "individual",
  "assetIds": ["asset-id-1"],
  "allowUpload": false,
  "allowDownload": true,
  "showMetadata": true,
  "expiresAt": "2024-12-31T23:59:59.000Z",
  "password": "optional-password"
}
```

**字段说明**:

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `type` | string | 是 | 分享类型: `individual` (单个资产) 或 `album` (相册) |
| `assetIds` | string[] | 是 | 要分享的资产ID列表 |
| `allowUpload` | boolean | 否 | 是否允许上传，默认 `false` |
| `allowDownload` | boolean | 否 | 是否允许下载，默认 `true` |
| `showMetadata` | boolean | 否 | 是否显示元数据，默认 `true` |
| `expiresAt` | string (ISO 8601) | 否 | 过期时间 |
| `password` | string | 否 | 访问密码 |

**成功响应 (201 Created)**:

```json
{
  "id": "string (UUID)",
  "key": "string (分享链接密钥)",
  "slug": "string (可选的友好URL)",
  "expiresAt": "string (ISO 8601)",
  "assets": [
    {
      "id": "string (UUID)"
    }
  ]
}
```

**分享链接格式**:

- 无 slug: `{baseUrl}/share/{key}`
- 有 slug: `{baseUrl}/share/{slug}`

### 参考资料

- [Immich 官方文档](https://immich.app/docs)
- [Immich API 文档](https://api.immich.app)
- [PicGo 插件开发指南](https://picgo.github.io/PicGo-Core-Doc/dev-guide/cli.html)
